This explains how the biodsl docker image is build and pushed to dockerhub:

# build biodsl docker image
docker build -t maasha/biodsl .

# push biodsl docker image to dockerhub
docker push maasha/biodsl
